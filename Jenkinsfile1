pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Checking Build System...'
            }
        }
        stage('Python Test') {
            steps {
                echo 'Testing Environment...'
                sh 'python3 --version || python --version'
            }
        }
    }
}
